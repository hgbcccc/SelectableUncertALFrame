# 不确定性度量方法

本文档展示了我们收集和实现的目标检测领域中的不确定性度量方法。这些方法都可以在 `SUAL/sual/core/uncertainty/metrics.py` 中找到对应的源代码实现。我们希望这些方法能为研究人员和工程师提供参考，同时也诚挚地欢迎社区对这些方法提出改进建议。

## 前言

不确定性度量在目标检测任务中扮演着重要角色，特别是在主动学习场景下。通过准确评估模型预测的不确定性，我们可以更好地理解模型的行为，指导样本选择策略。本文档收集和实现了多种不确定性度量方法，但我们深知这些方法仍有改进空间，也可能存在一些疏漏。我们真诚地欢迎社区成员对这些方法提出建议和指正。

## 实现说明

所有方法都在 `UncertaintyMetrics` 类中实现，主要包括以下几类：

1. 基础不确定性度量
2. 基于熵的不确定性度量
3. 基于方差的不确定性度量
4. 基于分位数的不确定性度量
5. 基于密度的不确定性度量
6. 价值度量方法
7. 基于检测框的不确定性度量
8. 空间遮挡率度量
9. 边缘采样不确定性度量
10. 最低置信度不确定性度量

每种方法都经过了基本的测试验证，但在实际应用中可能还需要根据具体场景进行调整和优化。

## 使用说明

您可以通过以下方式使用这些方法：

```python
from sual.core.uncertainty.metrics import UncertaintyMetrics
metrics = UncertaintyMetrics()
uncertainty_values = metrics.compute_uncertainty(result, methods='all')  # result 为mmdetection定义的一个DetDataSample
```

## 1. 基础不确定性 (Basic Uncertainty)

原文或者相关请点击[链接](https：xxxxxxx)

给定检测框的置信度集合 S = {s₁, s₂, ..., sₙ}，其中 sᵢ ∈ [0,1]，

基础不确定性计算如下：

    单个检测框不确定性：uᵢ = 1 - sᵢ

    最大不确定性：u_max = max{uᵢ}

    总体不确定性：u_sum = ∑uᵢ

    平均不确定性：u_avg = (1/n)∑uᵢspan

```python
def basic_uncertainty(self, result: DetDataSample) -> Dict[str, float]:
    """基础的不确定性计算(1-置信度)

    Args:
        result (DetDataSample): MMDetection的检测结果

    Returns:
        Dict[str, float]: 包含max/sum/avg三种不确定性度量的字典
    """
    scores = result.pred_instances.scores.cpu().numpy()
    uncertainties = 1 - scores

    return {
        'max_uncertainty': float(np.max(uncertainties)),
        'sum_uncertainty': float(np.sum(uncertainties)),
        'avg_uncertainty': float(np.mean(uncertainties))
    }

```

## 2. 熵不确定性 (Entropy Uncertainty)

原文或者相关请点击[链接](http：xxxxxx)

对于置信度集合 S，首先进行归一化处理转化为相对概率：

    归一化置信度：p̂ᵢ = sᵢ/∑sⱼ

然后计算熵值：

    熵：H = -∑(p̂ᵢ × log(p̂ᵢ))

    归一化熵：H_norm = H/log(n)

```python
def entropy_uncertainty(self, result: DetDataSample) -> Dict[str, float]:
    """基于熵的不确定性计算
  
    Args:
        result (DetDataSample): MMDetection的检测结果
  
    Returns:
        Dict[str, float]: 熵相关的不确定性度量
    """
    scores = result.pred_instances.scores.cpu().numpy()
    # 归一化scores
    norm_scores = scores / np.sum(scores)
    entropy = -np.sum(norm_scores * np.log(norm_scores + 1e-10))
  
    return {
        'entropy': float(entropy),
        'normalized_entropy': float(entropy / np.log(len(scores) + 1e-10))
    }
```

## 3. 基于统计的不确定性度量

原文或者相关请点击[链接](http：xxxxxx)

对于不确定性集合 U = {uᵢ} = {1 - sᵢ}，计算以下统计量：

    方差：σ² = (1/n)∑(uᵢ - μ)²

    标准差：σ = √σ²

    变异系数：CV = σ/μ

其中，μ 为不确定性的均值。

```python
def variance_uncertainty(self, result: DetDataSample) -> Dict[str, float]:
    """基于方差的不确定性计算

    Args:
        result (DetDataSample): MMDetection的检测结果
  
    Returns:
        Dict[str, float]: 方差相关的不确定性度量
    """
    scores = result.pred_instances.scores.cpu().numpy()
    uncertainties = 1 - scores

    return {
        'variance': float(np.var(uncertainties)),
        'std': float(np.std(uncertainties)),
        'cv': float(np.std(uncertainties) / (np.mean(uncertainties) + 1e-10))  # 变异系数
    }
```

## 4.分位数不确定性度量

原文或者相关请点击[链接](http：xxxxxx)

给定检测框的置信度集合 S = {s₁, s₂, ..., sₙ}，首先计算不确定性值：U = {uᵢ = 1 - sᵢ | sᵢ ∈ S}，将不确定性值从小到大排序：U' = {u'₁, u'₂, ..., u'ₙ}，其中 u'₁ ≤ u'₂ ≤ ... ≤ u'ₙ

然后计算以下统计量：

    Q₁ (25分位数)：P(X ≤ Q₁) = 0.25

    Q₂ (中位数)：P(X ≤ Q₂) = 0.50

    Q₃ (75分位数)：P(X ≤ Q₃) = 0.75

    IQR (四分位距)：Q₃ - Q₁

具体示例，假设有数据：[0.1, 0.4, 0.2, 0.3, 0.15]，**排序后**：[0.1, 0.15, 0.2, 0.3, 0.4]，

   k = (5-1) × 0.25 + 1 = 2

   Q₁ = 0.15

   k = (5-1) × 0.5 + 1 = 3

   Q₂ = 0.2

   k = (5-1) × 0.75 + 1 = 4

   Q₃ = 0.3

```python
def quantile_uncertainty(self, result: DetDataSample) -> Dict[str, float]:
    """基于分位数的不确定性计算
  
    Args:
        result (DetDataSample): MMDetection的检测结果
  
    Returns:
        Dict[str, float]: 分位数相关的不确定性度量
    """
    scores = result.pred_instances.scores.cpu().numpy()
    uncertainties = 1 - scores
  
    return {
        'q25': float(np.percentile(uncertainties, 25)),
        'q50': float(np.percentile(uncertainties, 50)),  # 中位数
        'q75': float(np.percentile(uncertainties, 75)),
        'iqr': float(np.percentile(uncertainties, 75) - np.percentile(uncertainties, 25))  # 四分位距
    }
```

给出

## 5. 密度不确定性度量

原文或者相关请点击[链接](http：xxxxxx)

密度不确定性度量主要关注两个方面：

1. 高不确定性检测框的比例
2. 不确定性值的分布密度

给定检测框的置信度集合 S = {s₁, s₂, ..., sₙ}，首先计算不确定性值：U = {uᵢ = 1 - sᵢ | sᵢ ∈ S}

R_high = |{uᵢ | uᵢ > 0.5}| / n  其中：|{uᵢ | uᵢ > 0.5}| 表示不确定性值大于0.5的检测框数量，n 为检测框总数

D = n / (max(U) - min(U) + ε) 其中：n 为检测框总数，max(U) 为最大不确定性值，min(U) 为最小不确定性值，ε 为小量(1e-10)，避免除零错误

- R_high 表示不确定性值大于0.5的检测框所占比例
- D 表示单位不确定性范围内的平均检测框数量

```python
def density_uncertainty(self, result: DetDataSample) -> Dict[str, float]:
    """基于密度的不确定性计算
  
    Args:
        result (DetDataSample): MMDetection的检测结果
  
    Returns:
        Dict[str, float]: 密度相关的不确定性度量
    """
    scores = result.pred_instances.scores.cpu().numpy()
    uncertainties = 1 - scores
  
    # 计算高不确定性(>0.5)的比例
    high_uncertainty_ratio = np.mean(uncertainties > 0.5)
  
    return {
        'high_uncertainty_ratio': float(high_uncertainty_ratio),
        'uncertainty_density': float(len(uncertainties) / (np.max(uncertainties) - np.min(uncertainties) + 1e-10))
    }
```

## 6. 价值度量方法

该方法基于[Active Teacher for Semi-Supervised Object Detection](https://arxiv.org/abs/2303.08348)论文中提出的三个关键因素：难度、信息量和多样性来评估样本的价值。

给定检测结果，通过综合评估以下三个维度来计算样本价值：

1. 难度（Difficulty）：基于预测分数的熵
2. 信息量（Information）：基于置信度的平均值
3. 多样性（Diversity）：基于类别分布

### 难度分数

H = -∑(p̂ᵢ × log(p̂ᵢ))

difficulty = H / log(n)

其中：p̂ᵢ = sᵢ/∑sⱼ 为归一化的置信度，n 为检测框数量

### 信息分数

information = (1/n)∑sᵢ，其中 sᵢ 为原始置信度

### 多样性分数

diversity = num_unique_classes / total_classes,针对自己的数据集来修改total_classes，

```python
def compute_value_metrics(self, result: DetDataSample) -> Dict[str, float]:
    """计算图片价值的三个指标：难度、信息量和多样性
  
    Args:
        result (DetDataSample): MMDetection的检测结果
  
    Returns:
        Dict[str, float]: 包含三个指标和综合得分的字典
    """
    # 获取所有检测框的信息
    scores = result.pred_instances.scores.cpu().numpy()
    labels = result.pred_instances.labels.cpu().numpy()
  
    # 检查是否有检测结果
    if len(scores) == 0:
        return {
            'difficulty_score': 0.0,
            'information_score': 0.0,
            'diversity_score': 0.0,
            'total_value_score': 0.0,
            'num_objects': 0,
            'num_classes': 0,
            'class_distribution': {}
        }
  
    # 1. 难度指标 (基于熵)
    norm_scores = scores / (np.sum(scores) + 1e-10)
    difficulty = -np.sum(norm_scores * np.log(norm_scores + 1e-10))
    difficulty = difficulty / (np.log(len(scores) + 1e-10))  # 归一化熵
  
    # 2. 信息指标 (基于所有检测框的置信度)
    information = np.mean(scores)
  
    # 3. 多样性指标 (基于类别数量)
    unique_classes = np.unique(labels)
    num_classes = len(unique_classes)
    diversity = num_classes / 80.0  # 假设COCO数据集80类，归一化
  
    # 4. 综合得分 (三个指标的加权和)
    total_score = (difficulty + information + diversity) / 3.0
  
    # 5. 统计类别分布
    class_distribution = {
        int(label): int(np.sum(labels == label)) 
        for label in unique_classes
    }
  
    return {
        'difficulty_score': float(difficulty),
        'information_score': float(information),
        'diversity_score': float(diversity),
        'total_value_score': float(total_score),
        'num_objects': int(len(scores)),
        'num_classes': int(num_classes),
        'class_distribution': class_distribution
    }
```

## 7. 基于检测框的不确定性度量

该方法通过分析检测框的空间特征来评估不确定性，包括框的重叠度和中心点分布特征。论文或者相关点击[链接]()

给定一组检测框 B = {b₁, b₂, ..., bₙ}，其中每个框 bᵢ = [x₁, y₁, x₂, y₂]，通过以下三个维度评估不确定性：

1. IoU不确定性：基于框间重叠度
2. 中心点熵：基于框中心点分布
3. 中心点方差：基于框中心点的离散程度

### IoU不确定性

对于任意两个框 bᵢ, bⱼ：

IoU(bᵢ, bⱼ) = intersection / union

iou_uncertainty = (∑IoU(bᵢ, bⱼ)) / num_pairs

### 2. 中心点熵

1.计算框中心点：center_x = (x₁ + x₂) / 2，center_y = (y₁ + y₂) / 2

2.将图像划分为 grid_size × grid_size 网格

3.计算中心点分布的熵：H = -∑(pᵢ × log₂(pᵢ)) ，其中 pᵢ 为每个网格中心点的概率密度

### 3. 中心点方差

center_variance = (var(center_x) + var(center_y)) / 2

```python
def box_uncertainty(self, 
                       result: DetDataSample, 
                       img_shape: Optional[tuple] = None) -> Dict[str, float]:
        """计算基于检测框的不确定性度量
  
        Args:
            result (DetDataSample): 检测结果
            img_shape (tuple, optional): 图片尺寸 (H, W)
  
        Returns:
            Dict[str, float]: 框相关的不确定性度量
        """
        bboxes = result.pred_instances.bboxes.cpu().numpy()
        if len(bboxes) == 0:
            return {
                'iou_uncertainty': 0.0,
                'center_entropy': 0.0,
                'center_variance': 0.0
            }

        # 计算IoU不确定性
        total_iou = 0
        num_pairs = 0
        for i in range(len(bboxes)):
            for j in range(i + 1, len(bboxes)):
                iou = self._calculate_iou(bboxes[i], bboxes[j])
                total_iou += iou
                num_pairs += 1
  
        iou_uncertainty = total_iou / (num_pairs + 1e-6)

        # 计算中心点
        centers = self._get_box_centers(bboxes)
  
        # 标准化中心点坐标
        if img_shape is not None:
            centers[:, 0] /= img_shape[1]  # 宽度归一化
            centers[:, 1] /= img_shape[0]  # 高度归一化

        # 计算中心点熵和方差
        center_entropy = self._calculate_center_entropy(centers)
        center_variance = self._calculate_center_variance(centers)

        return {
            'iou_uncertainty': float(iou_uncertainty),
            'center_entropy': float(center_entropy),
            'center_variance': float(center_variance)
        }
```

## 8. 空间遮挡率度量 (SOR)

空间遮挡率(Spatial Occlusion Rate, SOR)用于评估检测框之间的空间遮挡关系，反映目标间的空间交互程度。

给定一个检测框 b 和所有检测框集合 B，空间遮挡率定义为该框被其他框遮挡的面积比例。

对于检测框 b = [x₁, y₁, x₂, y₂]：SOR(b) = overlap_area / total_area。其中：overlap_area: 与其他所有框的重叠区域面积之和，total_area: 框b的总面积

max_sor = max{SOR(bᵢ)}, i ∈ [1,n]

avg_sor = mean{SOR(bᵢ)}, i ∈ [1,n]

sum_sor = ∑SOR(bᵢ), i ∈ [1,n]

其中：bᵢ 表示第i个检测框，n 为检测框总数  ，SOR(bᵢ) 表示第i个检测框的空间遮挡率，i ∈ [1,n] 表示遍历所有检测框

这三个指标分别反映：max_sor：最严重的遮挡情况；avg_sor：平均遮挡程度；sum_sor：累积遮挡效应

```python
def calculate_sor(self, result: DetDataSample) -> Dict[str, float]:
    """计算空间遮挡率(SOR)"""
    bboxes = result.pred_instances.bboxes.cpu().numpy()
    if len(bboxes) == 0:
        return {
            'max_sor': 1.0,
            'avg_sor': 1.0,
            'sum_sor': 1.0
        }

    sor_values = []
    for i in range(len(bboxes)):
        sor = self._calculate_sor(bboxes[i], bboxes)
        sor_values.append(sor)

    return {
        'max_sor': float(np.max(sor_values)),
        'avg_sor': float(np.mean(sor_values)),
        'sum_sor': float(np.sum(sor_values))
    }
```

## 9. 边缘采样不确定性 (Margin Sampling)

论文和相关点击链接[链接](https://www.zhihu.com/question/265479171/answer/1474978784)

边缘采样通过计算预测分数中最高分和次高分的差值来评估模型的不确定性。差值越小，表示模型对预测结果的区分度越低，不确定性越高。

给定检测框的置信度集合 S = {s₁, s₂, ..., sₙ}，其中 sᵢ ∈ [0,1]，令 s₁ ≥ s₂ ≥ ... ≥ sₙ 为排序后的置信度序列。

边缘不确定性计算如下：

    margin = s₁ - s₂

    normalized_margin = margin / s₁

```python
    def margin_uncertainty(self, result: DetDataSample) -> Dict[str, float]:
        """基于边缘采样的不确定性计算
  
        Args:
            result (DetDataSample): MMDetection的检测结果
  
        Returns:
            Dict[str, float]: 边缘采样相关的不确定性度量
        """
        scores = result.pred_instances.scores.cpu().numpy()
        if len(scores) < 2:  # 需要至少两个预测框
            return {
                'margin': 1.0,
                'normalized_margin': 1.0
            }
  
        # 对scores进行排序
        sorted_scores = np.sort(scores)[::-1]
        # 计算最高分和次高分的差值
        margin = sorted_scores[0] - sorted_scores[1]
        # 归一化margin
        normalized_margin = margin / sorted_scores[0] if sorted_scores[0] > 0 else 1.0
  
        return {
            'margin': float(margin),
            'normalized_margin': float(normalized_margin)
        }
```

## 10. 最低置信度不确定性 (Least Confident)

最低置信度方法直接使用模型预测的最高置信度来度量不确定性，置信度越低表示不确定性越高。

给定检测框的置信度集合 S = {s₁, s₂, ..., sₙ}，其中 sᵢ ∈ [0,1]：least_confident = 1 - max(S)，avg_least_confident = 1 - mean(S)

```python
    def least_confident_uncertainty(self, result: DetDataSample) -> Dict[str, float]:
        """基于最低置信度的不确定性计算
  
        Args:
            result (DetDataSample): MMDetection的检测结果
      
        Returns:
            Dict[str, float]: 最低置信度相关的不确定性度量
        """
        scores = result.pred_instances.scores.cpu().numpy()
        if len(scores) == 0:
            return {
                'least_confident': 1.0,
                'avg_least_confident': 1.0
            }
  
        # 计算1-最大置信度作为不确定性度量
        least_confident = 1 - np.max(scores)
        # 计算平均不确定性
        avg_least_confident = 1 - np.mean(scores)
  
        return {
            'least_confident': float(least_confident),
            'avg_least_confident': float(avg_least_confident)
        }
```

## 局限性与改进空间

我们认识到当前实现可能存在以下局限：

1. 部分方法可能需要更多的理论验证
2. 某些场景下的性能还有优化空间
3. 可能存在更多有效的不确定性度量方法

## 致谢

感谢所有为这些方法提供建议和反馈的研究者和工程师。您的意见对我们完善这些方法至关重要。如果您发现任何问题或有任何建议，请随时与我们联系。

## 贡献指南

我们非常欢迎社区成员以任何形式参与到这个项目中来：

1. 提出新的不确定性度量方法
2. 改进现有方法的实现
3. 补充文档和使用示例
4. 报告问题或提供建议

您可以通过以下方式参与：

- 提交 Issue
- 提交 Pull Request
- 发送邮件到：[邮箱号码]

## 参考文献

[待补充相关文献]

## 声明

本项目仍在持续改进中，欢迎各位专家和同行提出宝贵意见。如有任何问题或建议，请随时与我们联系。我们会认真对待每一条反馈，不断完善这些方法。

### 待实现的

[主动学习Active learning(五)——融合不确定性和多样性(SA, BADGE) - 知乎](https://zhuanlan.zhihu.com/p/428935939)

[主动学习（Active Learning） 概述、策略和不确定性度量 - 知乎](https://zhuanlan.zhihu.com/p/531696559)

[(1 封私信 / 8 条消息) 主动学习(Active learning)算法的原理是什么，有哪些比较具体的应用？ - 知乎](https://www.zhihu.com/question/265479171/answer/1474978784)
